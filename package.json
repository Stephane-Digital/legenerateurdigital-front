{
  "scripts": {
    "dev": "next dev",
    "build": "next build",           // ← simple et robuste
    "start": "next start",
    "lint": "next lint"
  },
  "engines": {
    "node": "20.x"                   // ← optionnel mais recommandé sur Vercel
  }
}
